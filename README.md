# Elliottwave-validator
The Elliott Wave validator has a script called trends_manager.py which has our main logic. The script relies on simple elliott wave patterns but this can be misleading and lead to false signals so the waves has our four validator methods inside of the TrendsManager Class.
